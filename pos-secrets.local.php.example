<?php
/**
 * Copy to /var/www/private/ilovepbj/pos-secrets.local.php (preferred)
 * or pos-secrets.local.php in this folder (dev only).
 *
 * Square: Developer Console → your app → OAuth
 *   https://developer.squareup.com/apps
 *   Redirect URL must match: https://ilovepbj.shop/pos/oauth/callback
 *
 * Clover: Developer Dashboard → your app → Site URL / Default OAuth
 *   https://www.clover.com/developers (or sandbox.dev.clover.com)
 *   Redirect URL must match: https://ilovepbj.shop/pos/oauth/callback
 *
 * Toast: Partner / API credentials from Toast integrations team
 *   https://doc.toasttab.com/doc/devguide/authentication.html
 *   Client-credentials grant (not seller browser OAuth).
 *
 * Aloha / Micros / others: no live API here — use CSV import presets at /admin/pos-import
 */
return [
    // Random 32+ char string used to encrypt tokens at rest (required for production)
    'token_cipher_key' => 'PASTE_LONG_RANDOM_STRING_HERE',

    // —— Square ——
    'square' => [
        'application_id' => 'sq0idp-REPLACE',      // or sandbox-sq0idb-...
        'application_secret' => 'sq0csp-REPLACE',  // keep secret
        'environment' => 'sandbox',                // sandbox | production
        // Optional override; defaults to https://{host}/pos/oauth/callback
        'redirect_uri' => '',
        // Webhooks: Developer Console → Webhooks → Signature Key
        // Notification URL: https://ilovepbj.shop/pos/webhook
        'webhook_signature_key' => '',
    ],

    // —— Clover ——
    'clover' => [
        'app_id' => 'REPLACE',       // App ID from Clover developer dashboard
        'app_secret' => 'REPLACE',   // App Secret
        'environment' => 'sandbox',  // sandbox | production
        'redirect_uri' => '',        // optional; defaults to /pos/oauth/callback
    ],

    // —— Toast (partner API / restaurant machine client) ——
    'toast' => [
        'client_id' => 'REPLACE',
        'client_secret' => 'REPLACE',
        // ws-api.toasttab.com (prod) or ws-sandbox-api.eng.toasttab.com
        'api_host' => 'ws-api.toasttab.com',
        'user_access_type' => 'TOAST_MACHINE_CLIENT',
    ],
];
