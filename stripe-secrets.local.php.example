<?php
/**
 * Copy this file to: stripe-secrets.local.php
 * Fill in your Stripe TEST secret key from:
 *   https://dashboard.stripe.com/test/apikeys
 *
 * Never commit stripe-secrets.local.php or share sk_live_ keys publicly.
 */
return [
    // Required — starts with sk_test_ while testing
    'secret_key' => 'sk_test_REPLACE_ME',

    // Optional — from Developers → Webhooks → signing secret (whsec_...)
    'webhook_secret' => '',

    // Optional publishable key (only needed if we add Stripe.js later)
    'publishable_key' => 'pk_test_REPLACE_ME',
];
